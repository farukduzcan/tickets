import 'package:flutter/material.dart';

class GDPData {
  final String text;
  final Color color;
  final String status;
  final int count;
  GDPData(this.status, this.count, this.color, this.text);
}
